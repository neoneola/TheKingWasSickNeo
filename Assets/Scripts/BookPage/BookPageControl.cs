using UnityEngine;
using System.Collections;

public class BookPageControl : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//renderer.material.shader = Shader.Find ("Custom/Transparent");
		//renderer.material.SetFloat ("_Mask/Offset", 0f);

		Debug.Log ("aaaa");

	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
